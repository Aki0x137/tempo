name: Update VERSION file

on:
  push:
      branches: [main]

jobs:
  update_version:
    runs-on: "ubuntu-latest"
    steps:
      - uses: actions/checkout@v4
      - name: Update VESRION file
        run: |
          make update-version